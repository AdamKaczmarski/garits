/*
package com.garits.report;

import org.hibernate.query.criteria.internal.expression.function.AggregationFunction;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;
import org.springframework.data.repository.RepositoryDefinition;
import org.springframework.stereotype.Repository;

import java.util.List;
import java.util.Map;

@Repository
public interface StatReports extends JpaRepository<StatReport,Long> {
  */
/*  @Query(value="select"+new com.garits.report.StatReport(v.answer,AggregationFunction.COUNT(v))+" as from customers v", nativeQuery = true)
    List<StatReport> getSomething();*//*

}
*/
